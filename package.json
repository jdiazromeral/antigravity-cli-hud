{
  "name": "antigravity-cli-hud",
  "version": "1.5.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "npx oxlint --deny-warnings",
    "demo": "esbuild scripts/demo.ts --bundle --platform=node --format=esm --outfile=scripts/demo.js && node scripts/demo.js",
    "build": "esbuild src/index.ts --bundle --platform=node --format=esm --outfile=dist/index.js && esbuild src/title.ts --bundle --platform=node --format=esm --outfile=dist/title.js && esbuild src/stats.ts --bundle --platform=node --format=esm --outfile=dist/stats.js && esbuild src/audit.ts --bundle --platform=node --format=esm --outfile=dist/audit.js",
    "stats": "node dist/stats.js",
    "audit:agy": "node dist/audit.js",
    "sync": "npm run build && node scripts/sync_installed_plugin.js"
  },
  "keywords": [],
  "author": "Javier Diaz",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "esbuild": "^0.28.1",
    "oxlint": "^1.79.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.8"
  }
}
